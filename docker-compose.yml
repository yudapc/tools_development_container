version: "3.1"

networks:
  default:
    external:
      name: development

services:
  postgres:
    extends:
      file: postgres.yml
      service: postgres

  redis:
    extends:
      file: redis.yml
      service: redis

  sonarqube:
    extends:
      file: sonarqube.yml
      service: sonarqube

  mysql:
    extends:
      file: mysql.yml
      service: mysql
